//  all this file does right now is import the User model and export an object with it as a property

const User = require("./User");
const Post = require("./Post");

module.exports = { User, Post };
